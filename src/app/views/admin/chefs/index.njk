{% extends 'admin/layout.njk' %};
{% block content %}
<div class="content">
<div class="header flex">
        <h1>Gerenciar Chefs</h1>
        <a class="button h-40" href="chefs/create">Criar Novo Chef</a>
    </div>
<div class="cards">
    {% for recipe in recipes %}
    <div class="card">
        <div class="image">
            <img src="{{recipe.recipe_image}}" alt="{{recipe.name}}">
        </div>
        <div class="content-card">
            <div class="title">{{recipe.name}}</div>
            <div class="author"><p>por {{recipe.author}}</p></div>
            <div class="link">
                <a href="/admin/chefs/{{recipe.id}}">Visualizar</a>
            </div>
        </div>
    </div>
    {% endfor %}
</div>
</div>
{% endblock content %}